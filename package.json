{
  "name": "axmgmtsim",
  "version": "0.1.0",
  "private": true,
  "description": "AX 경영 시뮬레이션 — AI 전환 기업의 사업 확장 의사결정 시뮬레이터",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "validate": "tsx packages/data/src/validate.ts",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "sim": "tsx apps/cli/src/index.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "zod": "^3.24.1"
  }
}
